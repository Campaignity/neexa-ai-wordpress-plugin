#!/bin/bash
# Guard: never commit the local dev config
if git diff --cached --name-only | grep -q 'includes/config-local\.php'; then
    echo "ERROR: includes/config-local.php is staged."
    echo "       Remove it with: git restore --staged includes/config-local.php"
    exit 1
fi
